[project]
name = "obamacares-agent"
version = "0.1.0"
description = "Obama Cares voice agent for anti-phishing security training"
requires-python = ">=3.11"
dependencies = [
    "line-sdk>=0.1.0",
    "httpx>=0.27.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
